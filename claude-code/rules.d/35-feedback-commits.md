## One commit per feedback item

When addressing PR feedback, one commit per feedback item, its message naming what the comment asked for, so each response verifies on its own and a wrong fix reverts cleanly; never one batch commit for the round. Fuller PR-feedback flow (dispositions, where captured context belongs): the `pr-feedback` skill.
