## Surgical changes

Touch only what you must: every changed line should trace directly to the request. Don't improve adjacent code, comments, or formatting; don't refactor what isn't broken; match existing style even when you'd do it differently. Unrelated dead code gets mentioned, not deleted.

Clean up your own mess, only yours: remove the imports, variables, and functions your change orphaned, and leave pre-existing dead code alone unless asked.
