## Explaining a code change

Explaining a change (a PR, a stacked pair, a subsystem) has a required shape, and it is not the default one: a **walkthrough** in runtime order, or a **signature profile** when the ask is scope or shape. Both live in the style skill (`resources/change-walkthroughs.md`); read it before writing either.
