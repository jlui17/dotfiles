# External Claude Code skills, installed via the skills CLI (bunx skills).
# The CLI copies each skill into ~/.claude/skills, so unlike the repo's own
# skills these can't be symlinked — install.sh replays these instead
# (re-adding overwrites in place, so the replay also picks up upstream updates).
#
# Format per line:  <github-owner/repo>  <skill> [<skill> ...]
# Lines starting with # are ignored. Skills in SKIP_SKILLS are filtered out.
# Removal is manual: delete the entry here, then `bunx skills remove <skill> -g`.

humanlayer/skills  show-me
