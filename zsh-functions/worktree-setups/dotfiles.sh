# Contract (this file doubles as the template for new repos): wt_setup runs
# inside the fresh worktree with REPO_ROOT, WORKTREE_PATH, and BRANCH set;
# an optional wt_dir=<repo-relative dir> overrides the .worktrees default.
wt_setup() {
  :  # dotfiles needs no per-worktree setup
}
