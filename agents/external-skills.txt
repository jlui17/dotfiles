# External Claude Code and Codex skills, installed via the skills CLI (bunx skills).
# The CLI keeps one universal copy and links it into both agents, so unlike the
# repo's own skills these aren't repo symlinks — install.sh replays these instead
# (re-adding overwrites in place, so the replay also picks up upstream updates).
#
# Format per line:  <source>  <skill> [<skill> ...]
# Source may be a GitHub owner/repo pair or a well-known skill URL.
# Lines starting with # are ignored. Skills in SKIP_SKILLS are filtered out.
# Removal is manual: delete the entry here, then `bunx skills remove <skill> -g`.

humanlayer/skills  show-me
vercel-labs/agent-browser  agent-browser
jlui17/mdnote  mdnote
https://cli.sentry.dev  sentry-cli
